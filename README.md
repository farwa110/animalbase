These are the two versions of animal base

animals_basic is the original, using just the JSON objects

animals_objects is the new and improved, but incomplete.
Your job is to add code to animals_objects, so it works.
The HTML is complete. You only need to work with the JS.

--filtering
try hardcoded filter only cats.
try hardcoded filter only dogs.
try hardcoded filter all.
add userinteractivity.
add some cleanup.

--sorting
try hardcoding sorting by type.
try hardcoding sorting by name.
add user interactivity.
add a more generic dort of function?
change sort direction.
add some cleanup.

--sorting and filtering.
combine sorting n filtering.
other fixes after that.
